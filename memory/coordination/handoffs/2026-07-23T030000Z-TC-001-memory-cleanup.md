# Pending Delta: TC-001 Memory Cleanup

Metadata:

- timestamp: `2026-07-24T01:46:25Z`
- agent: `/root/tc001_developer`
- role: developer
- task_id: `TC-001`
- base_commit: `650460360913c3b83c9cd4b706788ec0f82d55ac`
- evidence_paths:
  - `.codex-coordinator/runs/20260723T023929Z-b2258e1d/reports/STAGE-0/release-evidence-r1.md`
  - `.codex-coordinator/runs/20260723T023929Z-b2258e1d/reports/TC-001/memory-cleanup-report.md`
  - `.codex-coordinator/runs/20260723T023929Z-b2258e1d/reports/TC-001/scoped-governance-files.tsv`
  - `.codex-coordinator/runs/20260723T023929Z-b2258e1d/reports/TC-001/duplicate-groups.tsv`

## Changed

- Git-verified task tally is 42 Accepted and 7 Superseded cards, not 34
  Accepted.
- `src/services/aiPlayer.ts` is absent and its cleanup card/review are
  Accepted/PASS; it is not pending work.
- The six bundled speech JSON files total 8,521 entries, not 11,449.
- Current Function source uses `.cjs`; two equal-hash `.js` aliases were removed
  with retained-copy proof.
- Stage 0's exact production commit is
  `650460360913c3b83c9cd4b706788ec0f82d55ac`.
- Stage 0 GitHub Actions run `30053679882` failed at the guarded build and
  skipped deploy. Netlify Git integration separately published exact-commit
  deploy `6a62a55064a90100088318db`.

## Decisions

- Historical reports/cards remain evidence, not current state.
- Speech-library provenance/license and provider-adapter production status are
  `未验证`; do not convert conflicting historical claims into current facts.
- Preserve all unique/uncertain reports, ADRs, research, archives, generated
  history, and dual-platform skills.

## Evidence

- Local `HEAD`, `main`, and `origin/main` matched the base commit before edits;
  tracked baseline was clean.
- Card status tally: 42 Accepted, 7 Superseded, 0 other.
- JSON lengths: 3,142 + 2,938 + 765 + 762 + 481 + 433 = 8,521.
- The two Function duplicate groups and full digests are in
  `duplicate-groups.tsv`.

## Blockers

- None for the four enumerated canonical integration targets below.
- `memory/product-brief.md` is coordinator-owned but is not listed in TC-001's
  canonical integration target allowlist. Apply its one-line proposal only if
  the coordinator confirms that the handoff authorization covers it; otherwise
  retain it as an explicit follow-up.

## Next

1. Confirm canonical files still match the base commit versions read by this
   developer; rebase this delta rather than overwriting if they drifted.
2. Apply the evidence-backed canonical changes below.
3. Run memory validation/audit, full tests, and build.
4. Route the combined direct and canonical diff to the independent tester.
5. After PASS, create and record the Wave 1 baseline commit.

## Canonical Targets

### `memory/coordination/PROJECT_STATE.md`

- Set last verified baseline to Stage 0 evidence on 2026-07-24.
- Replace the 34-card statement with 42 Accepted and 7 Superseded tracked task
  cards.
- Replace local/production commit claims with exact Stage 0 commit
  `650460360913c3b83c9cd4b706788ec0f82d55ac`.
- Record the Stage 0 test baseline as 391 passed / 5 skipped, subject to the
  coordinator's post-integration rerun.
- Replace the manual-only/auto-deploy-off statement with: GitHub Actions run
  `30053679882` failed before deploy because the guarded build lacked
  `VITE_TURNSTILE_SITE_KEY`; Netlify Git integration published exact-commit
  production deploy `6a62a55064a90100088318db`.
- Replace `genai-proxy.js` with `genai-proxy.cjs`; list only
  `payment-escrow.cjs`; do not list deleted `.js` aliases.
- Mark provider-adapter production status `未验证` unless new endpoint-specific
  evidence is supplied.
- Remove `legacy-ai-player-cleanup`, `type-safety-cleanup`, `seo-robots`, and
  `netlify-csp` from pending/current task-pool sections because their tracked
  cards are Accepted.

### `memory/progress-report.md`

- Advance the update marker to the Stage 0/TC-001 checkpoint.
- Remove Accepted cards `legacy-ai-player-cleanup`, `type-safety-cleanup`,
  `seo-robots`, and `netlify-csp` from pending waves.
- Keep actual remaining work beginning with speech placeholder resolution,
  display-language purity, katakana entities, cloud TTS, and visibleText cleanup
  as planned/unverified work rather than completed state.
- Replace `provider-adapter.js ... 已部署` with source-present status and
  `生产部署状态未验证` unless endpoint-specific evidence is added.
- Replace the stale CLI-token/auto-deploy claim with the Stage 0 Git integration
  evidence; current CLI authentication state remains `未验证`.

### `memory/project-overview.md`

- Replace `netlify/functions/genai-proxy.js` with
  `netlify/functions/genai-proxy.cjs`.
- Replace the pending legacy-file description with: the old
  `src/services/aiPlayer.ts` implementation was removed; active AI entry remains
  `src/ai/aiOrchestrator.ts`; do not reintroduce the legacy path.
- Replace 11,449 with the Git-observed bundled count 8,521 and mark external
  corpus provenance/license `未验证`.

### `memory/coordination/WORKFLOW.md`

- Replace the pending legacy cleanup statement with: old
  `src/services/aiPlayer.ts` is removed; do not reintroduce it or use it as a
  feature base.

### `memory/product-brief.md` (scope-aware proposal)

- Replace `netlify/functions/genai-proxy.js` with
  `netlify/functions/genai-proxy.cjs`. No product goal or architecture behavior
  changes.

### `memory/INDEX.md` and task cards

- No content change proposed. The index ownership model remains valid, and the
  49 task-card statuses already provide the Git-verified tally.
